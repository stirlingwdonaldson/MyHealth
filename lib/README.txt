Place permitted dependency jars here if you do not set environment variables.

Required to run the app:
- sqlite-jdbc.jar

Required to run the JUnit tests:
- junit-platform-console-standalone.jar

JavaFX is supplied as a JavaFX SDK folder. Set JAVAFX_HOME to that SDK folder.
