
make interface for views.

root. 
- type should be generic, implements type pane 
- (assuming it's the parent of StackPane, etc.)


- add default values, e.g. for padding (like globals.css)? is that even possible in an interface?
- 


interface or abstract class? 
Probably interface, it's like a contract for API's. It's just making sure the format is right for other parts of the application

